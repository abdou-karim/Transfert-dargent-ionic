import { FormatSoldePipe } from './format-solde.pipe';

describe('FormatSoldePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatSoldePipe();
    expect(pipe).toBeTruthy();
  });
});
